{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "_comment_buildCommand": "npm run build, NOT vite build — package.json's build is `tsc -b && vite build`, so calling vite directly skipped the typecheck entirely and type errors shipped to production.",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
